# Exact pins for bit-reproducing the paper's pass@k generation runs.
# python 3.10; torch from the cu124 index:
#   pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu124
torch==2.5.1
transformers==4.46.2
tokenizers==0.20.3
numpy==2.2.6
safetensors==0.7.0
huggingface_hub==0.36.2
accelerate==1.13.0
sentencepiece==0.2.1
# Needed once per fresh run to build <output_root>/<dataset>/tasks.json from
# the HF datasets (openai_humaneval / evalplus). The tasks.json digests in
# REPRODUCIBILITY.md pin the content, so this version is not numerics-critical.
datasets==4.8.3
# Scoring-execution deps: generated completions may import these at test time,
# and a module missing from the scoring environment turns an otherwise passing
# sample into an import failure (this shifts Dream-Coder rows; FlexMDM samples
# do not import beyond the core set). The published scoring env included them.
pytest==9.1.1
scipy==1.15.3
matplotlib==3.10.9
ipython==8.39.0
flask==3.1.3
beautifulsoup4==4.15.0
selenium==4.46.0
