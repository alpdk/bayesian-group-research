# FlexMDM training + evaluation (pip deps). Install PyTorch separately with a
# CUDA build matching your system (see environment.yml / README), then:
#   pip install -r requirements.txt
#
# Versions below are the ones used for the released checkpoint. Newer patch
# versions usually work, but transformers is pinned because the Dream-Coder
# remote code targets this line.
transformers==4.46.2
tokenizers==0.20.3
huggingface-hub==0.36.2
datasets==4.8.3
safetensors==0.7.0
accelerate==1.13.0
# hydra/omegaconf: the original runs used 1.4/2.4 dev builds; the code uses only
# stable APIs, so these are pinned to the current stable line for installability.
hydra-core==1.3.2
omegaconf==2.3.0
wandb==0.25.1
protobuf==6.33.6
sentencepiece==0.2.1
regex==2026.2.28
psutil==7.2.2
numpy==2.2.6
pyyaml==6.0.3
tqdm
requests

# Scoring-execution deps: generated completions may import these at test time;
# a module missing from the scoring environment turns an otherwise passing
# sample into an import failure. See evals/requirements-repro.txt for the
# exact pins used for the published tables.
pytest
scipy
matplotlib
ipython
flask
beautifulsoup4
selenium

# Optional: flash-attention (faster training/inference; config
# model.attn_implementation=flash_attention_2). Install after torch:
#   pip install --no-build-isolation flash-attn==2.8.3
